[project]
name = "gameorganize"
dynamic = ["version"]
dependencies = [
    "pytest", 
    "flask", 
    "requests",
    "typing_extensions",
    "flask_sqlalchemy",
    "coverage"
]
requires-python = ">= 3.8"
